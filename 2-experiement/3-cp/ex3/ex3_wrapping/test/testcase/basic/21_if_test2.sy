// test if-else-if
int ifElseIf() {
  int a;
  a = 5;
  int b;
  b = 10;
  if(a == 6 || b == 0xb) {    // L1, L2
    return a;
  }
  else {    // L3
    if (b == 10 && a == 1)        // L4 && L5
      a = 25;                     // L7
    else if (b == 10 && a == -5)  // L8 && L10
      a = a + 15;                 // L12
    else                          // L13
      a = -+a;
  }

  return a;     // L14
}

int main(){
  putint(ifElseIf());
  return 0;
}

// mv	a0,t1
// j	.ifElseIf_end